internal class Program
{
    private static void Main(string[] args)
    {
        int line, column;
        int columnCounter = 0;
        int startPositionCounter = 0;
        int numberFirstColumn = 0;
        int numberSecondLine = 1;
        int sumSecondLine = 0;
        int multiplicationFirstColumn = 1;
        int shiftLineForUser = 1;

        Console.Write("Введите чилсо строк массива: ");
        line = Convert.ToInt32(Console.ReadLine());

        Console.Write("Введите число столбцов массива: ");
        column = Convert.ToInt32(Console.ReadLine());

        int[,] arrayOfNumbers = new int[line, column];

        for (int i = 0; i < arrayOfNumbers.GetLength(0); i++)
        {
            Console.WriteLine($"Введите строку {i + shiftLineForUser}: ");

            for (int j = 0; j < arrayOfNumbers.GetLength(1); j++)
            {
                arrayOfNumbers[i, j] = Convert.ToInt32(Console.ReadLine());
            }

            Console.WriteLine();
        }
        
        foreach (int i in arrayOfNumbers)
        {
            Console.Write(i + " ");

            columnCounter++;

            if (columnCounter == column)
            {
                Console.WriteLine();

                columnCounter = startPositionCounter;
            }
        }

        for (int i = 0; i < column; i++)
        {
            sumSecondLine += arrayOfNumbers[numberSecondLine, i];
        }
        
        for (int i = 0; i < line; i++)
        {
            multiplicationFirstColumn *= arrayOfNumbers[i, numberFirstColumn];
        }

        Console.WriteLine($"Произведение первого столбца: {multiplicationFirstColumn}");
        Console.WriteLine($"Сумма второй линии: {sumSecondLine}");
    }
}
